import { useEffect, useState } from 'react'
import { users } from './users/users' // --> get data from
import { DisplayHandler } from './users/users-handler' // --> pass data too

export const Handler = ({ user, handleUser }) => {
    // ---> users is the state and handleUser is callback function
}