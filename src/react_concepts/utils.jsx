export const Custom_Input = ({ name, value, onChangeInput }) => {

    return (
        <>
            <input
                value={value}
                onChange={onChangeInput}
                />
      
        </>
    )
}